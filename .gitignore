# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
venv/
.venv/

# Node / Frontend
node_modules/
dist/
.next/
.vite/
*.tsbuildinfo

# Runtime data
runtime_logs/
projects/
/jobs/
demo_output/
voice_bank/
uploads/

# 例外：frontend-next 里的 (app)/projects/ 和 (app)/jobs/ 是 Next.js 路由页面源码，
# 与顶层 runtime 数据目录同名但完全不是同一类东西，必须入库。
!frontend-next/src/app/(app)/projects/
!frontend-next/src/app/(app)/projects/**
!frontend-next/src/app/(app)/jobs/
!frontend-next/src/app/(app)/jobs/**

# Config (secrets)
.env
.env.*
# But keep the (secret-free) template — host-level placeholders, no real values.
!.env.example
autodub.local.json
remote_workbench.local.json
voice_registry.json

# Build artifacts
tools/caddy/
*.tar.gz

# Temp files
tmp_*
tmp_remote_env
tmp_remote_env.bak_*
tmp_playwright/
test_login.py
ssh_upload.py

# Claude Code
.claude/
.superpowers/

# CodeX 评审 scratch 目录（拉源码 / 临时 tarball / 子代理工作区）
# 纯本地协作产物，每个会话都不一样，永远不入库
.codex_tmp/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
.gstack/
.tmp-job-api-debug/
build/
.gitnexus
