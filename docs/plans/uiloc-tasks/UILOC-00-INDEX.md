# 界面语言切换（UI Page Locale）· 任务单元总索引（UILOC-00）

> 母方案：[`../2026-06-25-ui-page-locale-switch-plan.md`](../2026-06-25-ui-page-locale-switch-plan.md)（canonical，v3）。
> 本目录把母方案拆成可独立派发、可独立成 PR 的任务单元，供 **ship-unit** 单元循环逐个收口。
> **本阶段只产出文档、不执行代码。**
>
> **轴区分（最重要）**：本系列是 **UI/界面语言**（chrome 中⇄英），与产品 **`target_language`/配音方向**（`docs/plans/2026-04-15-i18n-target-language-direction.md` + `2026-06-13-multilingual-mutual-translation-plan-v3.md`）是**两条正交轴**，勿混（母方案 §0.1）。
>
> **状态（2026-06-25）**：M1 四单元（UI-01..04）已 flesh 到可执行；Phase 2/3 单元（UI-05..09）+ 后端轨（UI-BE-01）为**枚举占位/指针**，待项目主在母方案 §9.2 决策后细化。

## 如何使用（配合 ship-unit）

1. 按 **Wave 顺序**推进；同 Wave 内「可并行=是」的单元可分给不同 agent，各自**独立 worktree + feature 分支**（遵守 [CLAUDE.md 多 agent git 协作模型](../../../CLAUDE.md)：禁止多 agent 共用工作树做改状态操作）。
2. 认领单元 → 读该单元文档（先读母方案对应 §）→ 建 `uiloc/...` 分支 → 按 Step 执行（每步先过该步验收）→ 全单元 DoD 达标 → 多 lens + CodeX CLI + `@codex` bot + CI 绿 → 项目主 review → squash-merge → 勾掉本索引状态 + 回填实施 LOG。
3. 每个单元文档 **Step 0 都是「确认现状」**：执行时先复核 `file:line`（多 agent 并行，行号会漂移），以实际代码为准。
4. **命令环境**：默认 **Git Bash / CI Linux**；PowerShell 执行者改等价命令（`grep`→`Select-String`、`test -f`→`Test-Path`、`tail`→`Select-Object -Last`）。
5. **里程碑边界=人工门**：ship-unit 跑完 **M1**（UI-01..04）后**停在边界报项目主验收**，不自主跨入 Phase 2（Wave U-B 受 §9.2 决策门约束）。

## 状态图例

`☐ 待开始` ｜ `◐ 进行中` ｜ `✅ 已完成` ｜ `⏸ 阻塞/决策门`

## 任务单元清单

### Wave U-A — M1（基础设施 + 营销 + 最小 Auth；**已 flesh，可派发**）

| 单元 | 文档 | 状态 | 母方案 Phase | 工时 | 可并行 | 建议分支 |
|---|---|---|---|---|---|---|
| UI-01 | [i18n 基础设施](UI-01-i18n-foundation.md) | ✅ | P0a | M | 否（先行） | [PR #46](https://github.com/sun9bear/AITransVideo/pull/46) 已合并 |
| UI-02 | [路由迁移 + proxy + 切换器](UI-02-locale-routing-migration.md) | ✅ | P0b | L | 否（依 UI-01，spike 先行） | [PR #48](https://github.com/sun9bear/AITransVideo/pull/48) 已合并（squash `56404a55`） |
| UI-03a | [营销·结构化文案抽取](UI-03-marketing-en-seo.md)（§子单元拆分） | ✅ | Phase 1·T1.1 | M | 是（依 UI-02） | [PR #50](https://github.com/sun9bear/AITransVideo/pull/50) 已合并（squash `f30c1506`） |
| UI-03b | [营销·内联重文案 hero/pricing/trial](UI-03-marketing-en-seo.md) | ✅ | Phase 1·T1.2 | M | 是（依 UI-03a） | [PR #53](https://github.com/sun9bear/AITransVideo/pull/53) 已合并（squash `c26bd43e`）。含**收尾**：补完三页渲染的 6 leak 组件（trial-details/primary-cta/plan-card-cta/anonymous-trial-launcher【占位分支】/**hero-sample-player/seal-stamp** 后两者原卡又漏列、核渲染链揪出）。AnonymousTrialPanel（flag-ON consent 漏斗）延后独立单元 |
| UI-03c | [营销·legal 人审](UI-03c-legal-human-review.md)（HARD 人审） | ✅ | Phase 1·T1.2 | M | 是 | [PR #70](https://github.com/sun9bear/AITransVideo/pull/70) 已合并（squash `b1cd5ffb`，**项目主二轮人审已签**）。zh 源修 4 blocker（terms 7.2 非续费 / terms 10.4+privacy 10.1 删百度→通用第三方云存储+用户内容披露 / refund 9.2 原路退回 / terms 15.2 洪山区法院）+ terms/privacy/contact/legal-page **en bilingual 译文**（镜像 refund，非 dict）。bilingual = 对 legal 的**有意 R1 豁免**、legal 保持在 `localizedRoutes` **之外**（不挂 en hreflang） |
| UI-03d | [营销·SEO 翻旗 + EN 排版](UI-03-marketing-en-seo.md)（拆 d1/d2） | ✅ | Phase 1·T1.3 | M | 否（依 UI-03a+b+e/f 合并） | **d1 SEO 翻旗** [PR #63](https://github.com/sun9bear/AITransVideo/pull/63)（squash `1e93d6fc`）+ **d2 EN 排版** [PR #64](https://github.com/sun9bear/AITransVideo/pull/64)（squash `d3036b83`）。home/pricing/trial 互惠 hreflang + en metadata；legal 页 en hreflang 留 UI-03c |
| **UI-03e** | **首页区块英文化（上半）**：PainPoints/FeaturedDemos/ProductProof/WorkflowShowcase | ✅ | Phase 1·T1.2' | M | 是（依 UI-03a） | [PR #60](https://github.com/sun9bear/AITransVideo/pull/60) 已合并（squash `8ddf8690`） |
| **UI-03f** | **首页区块英文化（下半）**：Features/SuitedScenarios/TrustBanner/PricingPreview/FinalCta（link-button 无 CJK 不动）。⚠️ **primary-cta/plan-card-cta/anonymous-trial-launcher 已由 UI-03b 字典化、03f 只消费不再迁** | ✅ | Phase 1·T1.2' | M | 是（依 UI-03a/b，与 03e 串行 cjk-baseline） | [PR #61](https://github.com/sun9bear/AITransVideo/pull/61) 已合并（squash `d94f2099`） |
| UI-04 | [最小 Auth 英文化](UI-04-min-auth-en.md) | ✅ | Phase 1.5 | M | 是（依 UI-02） | [PR #49](https://github.com/sun9bear/AITransVideo/pull/49) 已合并（squash `85f2e7c3`） |

### ⚠️ 范围修正（2026-06-26 CodeX 审核 + 项目主决策）

**起因**：CodeX 审核指出 UI-03b「实现完成」是高估；核查发现**原 UI-03 方案卡的组件清单漏列了首页 ~9 个区块组件 + 几个 CTA + trial-details**。`/en` 首页实际渲染链 = Hero(英)→PainPoints(中)→FeaturedDemos(中)→ProductProof(中)→WorkflowShowcase(中)→Features(中)→SuitedScenarios(中)→ToolComparison(英)→TrustBanner(中)→PricingPreview(中)→Faq(英)→FinalCta(中)。**UI-03a+b 只英文化了 ~40% 营销表面**；`/en` 首页主体仍中文。

**项目主决策（AskUserQuestion 2026-06-26）**：
1. **范围 = 补全整个 /en 营销**（Q1）→ 新增 **UI-03e/UI-03f**（首页区块 + CTA 全英文化）；**UI-03b 必须先补完页内泄漏组件**（TrialDetails/CTA，否则其 hero/pricing/trial 页半中半英）再 PR。M1 体量随之增大（marketing 工作量约翻倍）。
2. **百度网盘从条款删除**（Q2）→ UI-03c legal 中文源：privacy 删除百度网盘专条；但**因管理员归档确会把用户上传/生成内容外流到运营方百度网盘（真实数据流），删除时须以「运营方可能使用第三方云存储归档已完成任务材料」的通用措辞兜底，避免少披露**（实施者注意，非纯删）。

**✅ M1 全部完成（2026-06-27）**：UI-01/02/03a/03b/03c/03d-1/03d-2/03e/03f/04 **全部合并 main**。`/en` 营销站内容全英文（首页全区 + hero/pricing/trial + FAQ/对比 + legal bilingual）+ /pricing·/trial 双语 SEO（互惠 hreflang）+ EN 衬线排版。**M1 里程碑边界已达——停等项目主验收，不自主跨入 Phase 2**（UI-05..09 受母方案 §9.2 决策门约束）。✅ **SEO 翻旗部署门已通过（2026-07-02 生产实测验收）**：/en 已随 CM-01/CM-04 全量部署上线，公网六页（/、/en、/{,en/}pricing、/{,en/}trial）hreflang 三件套互惠自指全对（恰好一个 x-default→zh）、canonical 全自指、robots.txt 源站版生效（含 /en 保护路由 disallow，CF 未拦截）、sitemap zh/en alternates 正确且 legal 页不挂 en、两 locale 都保留 google/msvalidate verification meta；gate ①=/en 首页零 chrome 中文泄漏（仅 demo 样片标题为中文，系 featured-demos-data.ts 记载的有意内容策略，非泄漏）；gate ②=en-support `availableLanguage=['zh-CN','en-US']` 已实测 live；auth 边界（/en/workspace 307→登录、login 页 noindex,nofollow）、未翻旗路由 /en/contact canonical 正确指回 /contact、/en/ 尾斜杠 308 归一均验证通过。剩 owner 一步：Google Search Console / Bing Webmaster 提交 sitemap + 请求索引（两家 verification meta 已在线，账号已存在）。

**新增延后项（UI-03b 发现，待排期）**：
- **AnonymousTrialPanel 本地化（独立单元，暂记 UI-03g）**：`anonymous-trial-panel.tsx`（954 行 / 119 CJK / flag `NEXT_PUBLIC_ENABLE_ANONYMOUS_PREVIEW`-ON 的匿名预览上传·预览·**consent 法务文案**漏斗）。consent 文案有法务权重，应与 03c legal 同等审慎，不混入 leak-fix。**因该 flag-ON 时 /en home 泄漏中文，@codex #66 P2-1 + 项目主决策已把 home `/` 移出 `localizedRoutes`（#67）→ /en home 不挂 en hreflang / 不进 sitemap en。** ⚠️ **UI-03g 落地（本地化 panel）后必须配套**：① `site.ts` 把 `/` 加回 `localizedRoutes`；② 移除 `hreflang-check.mjs` 的 home `/` 无-en 防回归断言 + `zh-snapshot.mjs` 的 `hreflang('/')` zh-only 断言（改回互惠含 en）；③ home `page.tsx`/`sitemap.ts` 的 `homeLocalized`/`localized` 分支自动恢复挂 languages（无需改）。
- **UI-03d 范围补充（@codex #53 P2 采纳）**：03d 不止 per-page `generateMetadata`，**还须 locale 化面包屑 JSON-LD（BreadcrumbJsonLd item 名）**——否则 `html lang="en"` 下 /en 的 `<title>`/分享预览/结构化数据仍中文。

### Wave U-B — Phase 2 工作台（**枚举占位，§9.2 决策门**）

| 单元 | 文档 | 状态 | 母方案 Phase | 工时 | 门 |
|---|---|---|---|---|---|
| UI-05 | [App 中央字典本地化](UI-05-central-dictionaries.md) | ✅ | Phase 2 · T2.1 | M | [PR #81](https://github.com/sun9bear/AITransVideo/pull/81) 已合并（squash `efa840ed`） |
| UI-06 | [always-on 用户页](UI-06-app-user-flows.md) | ◐ | Phase 2 · T2.2 | L | **part1 账户页 [PR #85](https://github.com/sun9bear/AITransVideo/pull/85) 已合并（squash `99511f6d`）**（projects/voices/settings/help/notifications/billing，332 key）；**part2 核心工作台**薄切片方案见 [UI-06-part2-workbench-slices](UI-06-part2-workbench-slices.md)（W1-W5）：**W1 任务详情→结果/下载 [PR #87](https://github.com/sun9bear/AITransVideo/pull/87) 已合（squash `805cb92c`）** + **W2a 创建表单 chrome [PR #88](https://github.com/sun9bear/AITransVideo/pull/88) 已合（squash `4c0a5d36`）** + **W2b consent/法务文案 [PR #89](https://github.com/sun9bear/AITransVideo/pull/89) 已合（squash `f5e3526e`）**（W2a ~110 CJK→appTranslationForm + W2b 13 consent 串→独立 appTranslationFormConsent；**TranslationForm 至此完全本地化 0 内联 CJK**；克隆/consent 门控逻辑零改；UI-09 chokepoint 接入） + **W3a Studio 审校决策面板 [PR #90](https://github.com/sun9bear/AITransVideo/pull/90) 已合（squash `056d6748`）**（W3 拆片：TranslationReviewPanel/SmartAutoDecisionPanel/VoiceReviewPanel ~90 CJK→3 namespace，UI-09 5 routings 含拆分失败 + helper 线程化） + **W3b Studio 音色选择+说话人管理 [PR #91](https://github.com/sun9bear/AITransVideo/pull/91) 已合（squash `c716f825`）**（VoiceSelectionPanel+SpeakerAudioAuditModal+EditPageSpeaker* ~86 CJK→4 namespace appVoiceSelection/appSpeakerAudit/appSpeakerCreate/appSpeakerBadge；含 CosyVoice 克隆门=**逻辑零改**付费红线保留；UI-09 6 routings + 模块级 helper/STATUS_MAP typed-key 线程化） + **W4a 克隆 modal + 段选择器 [PR #92](https://github.com/sun9bear/AITransVideo/pull/92) 已合（squash `d25e1468`）**（CosyVoiceCloneModal/VoiceCloneModal/CosyVoiceSegmentPicker ~91 CJK→3 namespace appCosyClone/appVoiceClone/appCosySegments；克隆门逻辑零改+provider 隔离；phase42 D2/E1/E2 49/49[顺带修 2 个 W3b 致 stale 的 E1 optgroup 守卫→locale-aware]；describeCloneError locale-conditional 修 CodeX+@codex P2 错误码本地化[zh 字节不变]） + **W4b CosyVoice 克隆授权 consent modal [PR #93](https://github.com/sun9bear/AITransVideo/pull/93) 已合（squash `178f1e9c`）**（CosyVoiceConsentModal 13 法务 consent 串→appCosyConsent，Claude 把关忠实翻译+2 法务疑点标注[en consent 挂 zh-anchored modal_version / PRC 法+境内数据框架]；CHECKBOX_CONFIG→typed key；version-pin/payload/gate 零改；guard-13 locale-agnostic 修；Workflow legal-faithful lens 抓 1 HIGH[封禁→ban]已修）；**至此 W4 完成（W4a+W4b）；W5 post-edit + errors.code.* 跟进单 待做** |
| UI-07 | [Intl 格式器参数化](UI-07-intl-formatters.md) | ✅ | Phase 2 · T2.3 | S | [PR #84](https://github.com/sun9bear/AITransVideo/pull/84) 已合并（squash `fe14353b`） |
| **CM-04** | support 客服组件英文化（无独立 UI-NN 文档；补母 plan Task 3.3 枚举缺口，见 [`docs/plans/2026-07-02-commercialization-sprint-plan.md`](../2026-07-02-commercialization-sprint-plan.md) §2 CM-04） | ✅ | Task 3.3（枚举缺口） | M | worktree `claude/cm04-support-en`。用户可见 5 组件（SupportWidget/SupportLauncher/SupportConversationPanel/SupportMessageList/SupportHandoffBanner）+ support-copy.ts → 新 namespace `appSupport`（37 leaf key，含 fallback.quickQuestions 4 项数组展开）；admin/ 3 文件 + AdminPresenceSwitcher.tsx + useAdminHeartbeat.ts 明确豁免（operator-only，不译）；cjk-baseline 4 文件 25 处内联 CJK 清零（只减不增）；5 个 uiloc 守卫 + eslint(0 error) + tsc(0 error) 全绿。server-emitted `/api/support/config` 内容不在本单元范围（归 UI-BE-01 后端轨） |

### Wave U-C — Phase 3 共享 UI + 错误层（**枚举占位，§9.2 决策门**）

| 单元 | 文档 | 状态 | 母方案 Phase | 工时 | 门 |
|---|---|---|---|---|---|
| UI-08 | [共享 UI 收尾](UI-08-shared-ui.md) | ⏸ | Phase 3 · T3.3 | M | §9.2 |
| UI-09 | [客户端错误层 + error-code map](UI-09-client-error-layer.md) | ✅ | Phase 3 · T3.4 | M | **always-on 层已合**（[PR #86](https://github.com/sun9bear/AITransVideo/pull/86) squash `2efcb57c`）：ApiError.errorCode + resolveBackendMessage + localizeApiError + `errors` namespace + 21 个 always-on 显示点。`code.*` deferral 到工作台切片（27 个 getErrorMessage 点待该切片路由）；耦合 UI-BE-01（未编码错误 en 仍漏中文=已知缺口） |

### 后端轨 — Phase 4（**指针，非前端 uiloc lane**）

| 单元 | 文档 | 状态 | 母方案 Phase | 工时 | 门 |
|---|---|---|---|---|---|
| UI-BE-01 | [后端 error-code envelope + Accept-Language](UI-BE-01-backend-error-codes.md) | ⏸ | Phase 4 | L | §9.2 Q7；归后端 backlog / 与代码质量后端轨协调 |

### 非单元（显式 out-of-scope，防误领）

- **Admin**（`app/[locale]/(app)/admin/**`）— operator-only，保持中文，不翻（随 route group 整体物理迁移但不动内容）。
- **post-edit 子树**（`workspace/[jobId]/edit/**`）— `NEXT_PUBLIC_ENABLE_POST_EDIT` 默认关，随该功能上线再议。
- **币种换算 / USD 定价显示** — 归 Paddle/billing 后续（母方案 §1.6 / §9.2 Q4）。
- **配音 `target_language`** — 归 multilingual-mutual-translation 工作（§0.1）。

## 文档直达

- M1：[UI-01 基础设施](UI-01-i18n-foundation.md) · [UI-02 路由迁移](UI-02-locale-routing-migration.md) · [UI-03 营销英文化（a/b/c/d 四子单元，见文内「子单元拆分」）](UI-03-marketing-en-seo.md) · [UI-04 最小 Auth](UI-04-min-auth-en.md)
- Phase 2：[UI-05 中央字典](UI-05-central-dictionaries.md) · [UI-06 用户页](UI-06-app-user-flows.md) · [UI-07 Intl 格式器](UI-07-intl-formatters.md)
- Phase 3：[UI-08 共享 UI](UI-08-shared-ui.md) · [UI-09 客户端错误层](UI-09-client-error-layer.md)
- 后端轨：[UI-BE-01 后端 error-code](UI-BE-01-backend-error-codes.md)

## 依赖关系（DAG）

```
UI-01 ──→ UI-02 ──┬──→ UI-03a ──→ UI-03b ──┐
  (P0a)   (P0b)    │      (结构化)   (hero/pricing)│
                  │    UI-03c（legal 人审，并行）  ├──→ UI-03d（SEO 翻旗，依 03a+03b）
                  │                              │
                  └──→ UI-04   （最小 Auth，依 UI-02）
                        UI-03* / UI-04 可在 UI-02 合并后并行；UI-03c 人审慢不阻塞 03a/b/d
            │
            └─ M1 里程碑边界（停，报项目主验收）──────────────
                                                              ↓ §9.2 决策后
UI-05 ──→ UI-06            UI-07（独立，依 UI-02）
  (中央字典先于用户页)     UI-08    UI-09 ←→ UI-BE-01（耦合）
```

- **M1 推进顺序**：UI-01 →（合并后）UI-02（**spike 先行**）→（合并后）UI-03a → UI-03b → UI-03d（SEO 翻旗），UI-03c（legal 人审）与 UI-04 并行 → **M1 边界停**。
- Phase 2/3：§9.2 拍板后开；UI-05 先于 UI-06；UI-06 须排在 **TU-11 / PR #38** 后（文件重叠，母方案 §0.5/§0.6）；UI-09 与 UI-BE-01 耦合（前端 code→串 等后端发 code）。

## 关键跨单元协调（实施前必读）

1. **UI-01 ↔ UI-02 紧急回滚耦合**：止血回滚（`routing.ts` 的 `locales` 收回 `['zh']`）改的是 **UI-01 文件**——UI-02 紧急回滚须与 UI-01 owner 协调。
2. **UI-02 ↔ UI-03 两处 owner 边界**：(a) title 模板**品牌后缀** locale 化归 **UI-02** 的 `[locale]/layout`，UI-03 只改 page 级 title 值，**避免双后缀**；(b) 字体变量 `--font-en-display` 注入点在 `[locale]/layout`，由 **UI-02** 拥有，UI-03 **append 不重排**。
3. **`<html lang>` zh-CN→zh-Hans 单点豁免（UI-02）**：是有意变更，会在「zh 字节一致快照」上产生 **1 处预期 diff**——须**显式登记单点豁免并经项目主确认**，不得静默掩盖红线 1。
4. **messages 形态统一（CodeX 二审 #1）**：M1 全线 **namespace-per-file** `messages/{zh,en}/{common,marketing,auth,seo}.json`；UI-01 的 `request.ts` 用**固定 namespace import + merge**（非 glob、非单文件）。UI-03 写 `marketing`/`seo`、UI-04 写 `auth`，与 UI-01 骨架一致。**任何单元都不得退回单文件 `messages/{locale}.json`**（会导致命名空间文件运行时不加载）。
5. **无 JS 测试运行器 → 统一 `uiloc:*` 脚本（CodeX 二审 #5）**：`frontend-next/` 无 vitest/jest（CI frontend job = lint + tsc + build）。守卫一律独立 node 脚本 + `npm run`，**统一命名**：`uiloc:cjk-guard` / `uiloc:zh-snapshot` / `uiloc:key-parity`（UI-01 立机制，UI-02/03/04 复用 + 按需 append 如 `uiloc:hreflang-check`）。**不得**写 `vitest`/`jest`/`npm test -- <name>` 等不存在的命令。
6. **post-UI-02 路径**：UI-03/UI-04 在 UI-02 之后执行，`(marketing)`/`(auth)` 已在 `app/[locale]/` 下；其文档为可读性仍写 `app/(marketing|auth)/...`，执行者 Step 0 须解析为 `app/[locale]/(...)`。
7. **paddle-checkout root layout（CodeX 二审 #3）**：UI-02 删顶层 `app/layout.tsx` 后，`app/paddle-checkout/` 必须自带 `app/paddle-checkout/layout.tsx`（locale-neutral root layout，多 root layout 模式），否则 build 报错——已写进 UI-02 Step 7（非 §9 决策）。
8. **shallow repo footgun**：本仓库 `.git` 是 shallow + 多 worktree 共享——**本地 shell 永不** `git fetch --depth=1`（会 graft HEAD 进共享 `.git/shallow` 致 push 被 reject）。守卫的 base-ref diff **只在 CI** 用 TU-03 的浅 fetch 模式（memory `feedback_shallow_repo_fetch_footgun`）。
9. **共享 CI/pre-commit append 不覆盖**：`.github/workflows/ci.yml` + `.pre-commit-config.yaml` 由代码质量 TU-03 建好；本系列只 **append**（复用「只阻断新增 / 读 base-ref 基线」模式），动前 rebase 最新 main（母方案 §0.6）。

## 统一文档模板（每个 flesh 单元遵循）

```
# UI-NN · <标题>
- 执行框架说明（ship-unit / checkbox / worktree / 显式 pathspec）
- 元信息表：目标 / 价值 / 关联(母方案 Phase+§) / 前置依赖 / 建议分支 / 预估工时
- ## 不在本单元范围（out-of-scope）
- ## 必守不变量（复述本单元相关红线）
- ## 执行步骤：Step 0 确认现状 → Step 1..N（每步：动作 + 文件 + 具体改法 + 该步验收[可机器验证命令]）
- ## 测试计划（新增 + 默认 zh 字节一致回归）
- ## 回滚方案（文件 / commit 边界 / git revert 优先）
- ## 完成定义 (DoD)（清单式可勾选）
- ## 关联（回母方案 + 依赖单元）
```

## 全局必守红线（所有单元通用）

- **付费 API 硬约束**：本系列**纯表现层**，**不得**新增/触碰任何 MiniMax clone / 付费 TTS / 付费 LLM / 付费 ASR 路径。
- **红线 1 默认 zh 字节一致**：每单元交付后默认中文 DOM/URL/SEO 标签逐字节不变（`<html lang>` zh-CN→zh-Hans 的单点豁免见上「关键协调 3」）。
- **红线 2 不碰 pipeline 语言字段**：UI locale ≠ `target_language`；不读写 `source_language`/`target_language`/`language_pair`/`cn_text`/voice `compatible_target_languages`。
- **红线 4 SEO anti-leak**：`canonical`/`hreflang`/OG title·description 永在 page 级，绝不进 root/`[locale]` layout。
- **红线 5 content 不译**：job 标题、转录、译文、voice 名、`display_title_zh`、说话人名一律透传。
- **红线 8 不自动重定向 + `localeDetection:false`**：URL 是唯一语言真源，cookie 不驱动裸 `/` 恢复。
- **单 `<html>`**：`<html>/<body>` 唯一落 `app/[locale]/layout.tsx`，顶层 `app/layout.tsx` 删除。
- **git**：独立 worktree + `uiloc/...` 分支；提交显式 pathspec（`git commit -- <files>`），**禁** `git add .`。

## 实施 LOG

| 日期 | 单元 | PR | 审查 | 结果 |
|---|---|---|---|---|
| 2026-06-25 | UI-01 i18n 基础设施 | [#46](https://github.com/sun9bear/AITransVideo/pull/46) squash `050722b2` | CodeX CLI（v4 AppConfig typo 保护，已修+探针验证）→ 多 lens（zh-snapshot node floor / cjk-guard fail-closed，已修）→ @codex bot（2×P2 守卫硬化：occurrence-count + env-independence，已修验证；1×P1 lockfile=false-positive，npm ci 实测 exit 0 已驳回）→ CI blocking 全绿 | ✅ 合并 main |
| 2026-06-25 | UI-02 路由迁移 + proxy + 切换器 | [#48](https://github.com/sun9bear/AITransVideo/pull/48) squash `56404a55`（rebase 过 TU-07） | 多 lens 8-agent 对抗评审：**1 critical**（/paddle-checkout 被 next-intl rewrite 进 /zh → authed 支付 handoff 404；`next start` 实测确认）+ **1 high**（workspace/[jobId]/page.tsx 单引号 import 被双引号 sed 漏切→丢 /en 前缀）→ 均修+运行态复验。@codex bot：2×P2（登录/登出后 locale 连续性）→ **转 UI-04**（UI-02 Step 3 显式把 window.location/登出留原生）。gate 全绿：build/lint(0 err)/cjk-guard(重生成多集证明纯 relocation 2447 occ net-new=0)/key-parity/zh-snapshot；proxy curl 矩阵全过（R8 不跳、auth locale 保留、authed paddle-checkout 200） | ✅ 合并 main |
| 2026-06-25 | UI-04 最小 Auth 英文化 | [#49](https://github.com/sun9bear/AITransVideo/pull/49) squash `85f2e7c3` | 实现委派子 agent（4 commit）+ 我独立复跑全 gate。多 lens 6-agent 对抗评审 **0 critical/high/medium，7 low**（均聚 post-auth-redirect.ts）→ 收敛：回环守卫 locale-aware（`/en/auth/login` 漏 `startsWith('/auth')`→en 漏斗登录回环；deLocalizePath 剥前缀后判，from verbatim，7-case 逻辑测过）+ 中文 throw/zh-snapshot 值级局限标注转 UI-09。@codex bot **0 findings**（tsc⚠=其 sandbox next-intl 解析失败=false neg，本地+CI tsc 绿）。gate 全绿：lint(0err)/tsc/build(8 路由 SSG)/key-parity(152)/cjk-guard(net-new=0,移除182occ)/zh-snapshot(含 auth 字节一致) | ✅ 合并 main |
| 2026-06-25 | UI-03a 营销结构化文案抽取 | [#50](https://github.com/sun9bear/AITransVideo/pull/50) squash `f30c1506` | 委派子 agent 实现（FAQ/对比表/nav/footer/SEO chrome 入 marketing+seo namespace；FAQ items 单源同喂 DOM+JsonLd；site-json-ld locale 驱动 zh 保 zh-only）。我独立复跑全 gate + 多 lens 4-agent 对抗评审 **0 critical/high/medium，5 low** → 收敛 en seo 双源同步守卫（messages/en/seo.json↔localeSeo.en）。@codex 0 findings。**移交/待确认**：company-info 常量随消费点 03b/03c 抽；seo namespace inert→03d 收敛单源；🔶 en JSON-LD availableLanguage=en-US 待项目主确认是否真有英文客服（03d go-live 前，否则去 en-US 保 ['zh-CN']） | ✅ 合并 main |
| 2026-06-25 | UI-03b hero/pricing/trial 内联重文案 | 分支 `uiloc/marketing-en-seo-b` 已推 `9c02c4f0`（未开 PR） | 委派子 agent 实现（hero rich-text/pricing 链路 ICU/trial + company-info 在 pricing-assurance 消费点字典化）。**我已独立复跑全 gate 全绿**：tsc 0 / lint 0err / build(pricing·trial SSG) / key-parity / cjk-guard(net-new=0，多集 2192→2120 移除72) / zh-snapshot(+14 串)。**未做：多 lens 对抗评审 + push-PR + @codex + merge** —— 本会话上下文已很深，按既定纪律停在干净检查点交新会话续（不降质硬推）。子 agent 2 裁定待评审核：①company-info 常量保留(仍被 03c legal/contact 消费)②trial-details.tsx 判 out-of-scope(卡只列 trial/page，需核是否致 trial 页半中半英) | ◐ 实现完成待评审 ship |
| 2026-06-26 | **CodeX 审核 + 范围修正**（见上「⚠️ 范围修正」§） | — | CodeX [P1]：UI-03b 非「实现完成」——`/en` 页仍露中文(TrialDetails/CTA)，应作 PR 前阻塞项。核查发现**原方案卡漏列首页 ~9 区块 + CTA + trial-details**，`/en` 首页主体仍中文(UI-03a+b 仅 ~40% 营销)。[P2] UI-03b 落后 main(TU-08/#51)，PR 前须 rebase。项目主决策：①补全整个 /en 营销→新增 UI-03e/f ②百度网盘从条款删除(通用第三方存储措辞兜底)。**纠正前述「M1 实现完成 4/4」高估** | 🔧 范围扩大，replan |
| 2026-06-27 | **UI-03b 收尾 + ship** | [#53](https://github.com/sun9bear/AITransVideo/pull/53) squash `c26bd43e` | rebase 到 main `228bc275`(整合 PayPal billing namespace) + 收尾 commit。**6 leak 组件**字典化(trial-details/primary-cta/plan-card-cta/anonymous-trial-launcher【占位分支】/**hero-sample-player/seal-stamp** 后两者原卡又漏列、核 hero 渲染链揪出)。我独立复核：zh 字节一致 **21/21**、cjk-baseline 只减不增(2120→2078)、全 gate 绿。**多 lens 5-agent 对抗评审 0 confirmed**。@codex **3×P2 均已知/有意出范围**(AnonymousTrialPanel 延后 + metadata/breadcrumb→03d)、**无 major**。**file-size-guard 红=PayPal 撑大 billing.py 的 main-state(非本 change)→ PR #54 `e44999d3` bump 基线后 re-run 转绿**（教训：CI 推理对 **origin/main** 非本地 worktree——本地 main 有别轨未推 credits commit 误判多 2 violator）。passthrough 守住:plan.display_name/demo.*/译印章 PNG | ✅ 合并 main |
| 2026-06-27 | **UI-03e 首页上半区英文化** | [#60](https://github.com/sun9bear/AITransVideo/pull/60) squash `8ddf8690` | 4 组件（pain-points/product-proof/workflow-showcase server→async getTranslations + featured-demos-client/featured-demo-card client useTranslations）→ 4 新 namespace。委派子 agent 实现 + 我独立复核：**全 50 条 zh 字典叶值 = HEAD 源逐字节一致**（自动核验，超出 9 条 snapshot pin）、cjk-baseline 只减不增(2078→2023, 0 net-new)、全 gate 绿(含 build 双 locale)。**多 lens 5-agent 对抗评审 0/0 confirmed**。@codex 无 major（⚠️=其 sandbox 缺 typescript dep，false neg）。passthrough：demo.display_name/品牌词/注释不动 | ✅ 合并 main |
| 2026-06-27 | **UI-03f 首页下半区英文化** | [#61](https://github.com/sun9bear/AITransVideo/pull/61) squash `d94f2099` | 5 组件（features/suited-scenarios/trust-banner/pricing-preview/final-cta 全 server→async getTranslations + t.raw；final-cta h2 t.rich br；features/trust-banner icon 移出为 index 数组）→ 5 新 namespace。我独立复核：**全 51 条 zh 叶值 = HEAD 源逐字节一致**（final-cta rich heading 经 JSX 空白折叠等价复核）、cjk-baseline 2023→1971(0 net-new)、全 gate 绿。**多 lens 5-agent（含 icon-pairing-order 专项）0/0 confirmed**。@codex「No blocking findings」。**至此 /en 首页全区内容英文化（除 SEO/metadata=03d）**。只消费不改 03b/03e 子组件；link-button 无 CJK 不动 | ✅ 合并 main |
| 2026-06-27 | **UI-03d-1 营销 SEO 翻旗** | [#63](https://github.com/sun9bear/AITransVideo/pull/63) squash `1e93d6fc` | home/pricing/trial（已整页英文）`static metadata → generateMetadata(locale)`：self-canonical（zh 保留相对串 byte-identical / en 绝对 /en）+ 互惠 hreflang + localized OG；`site.ts` 加 `localizedRoutes` + `hreflangLanguages` 路由感知（legal 只挂 zh）；layout 仅本地化 title 模板/keywords/OG locale（**R4：无 canonical/hreflang/OG-url**）；sitemap 单点走 hreflangLanguages；breadcrumb 加可选 locale prop；**新增 `hreflang-check` guard**；zh-snapshot 翻旗 `hreflang('/')` 含 en + 新增 `hreflang('/terms')` 无 en 防回归。委派子 agent + 我独立复核：6 条 zh seo 值 = HEAD 源逐字节一致、R4 grep clean、cjk-baseline 只减、全 gate 绿。**多 lens 5-agent SEO 对抗评审（hreflang 自指/R4/R1/Next16/guard）2 raw→0 confirmed**（仅注释计数 nit，已修）。**@codex 用量耗尽**（rate-limited）→ codex 限额 fallback：主模型 + 5-lens 终审。legal 页不动、留 UI-03c | ✅ 合并 main |
| 2026-06-27 | **UI-03d-2 EN 排版轨道** | [#64](https://github.com/sun9bear/AITransVideo/pull/64) squash `d3036b83` | /en `.ink-display` 换 EB Garamond 800（复用已加载变量字体，800 与 600 同 woff2=零新增二进制）替代 CJK 回退 Georgia faux-bold + 收紧 `.zh-body` 行距；layout 转 async+getLocale，`locale==="en"` 给 `.marketing-root` 追加 ` locale-en`（zh className byte-identical）；globals.css 沿用 `.ink-heading` 同款「单类+:not(.__never__)」(0,3,0) 规避 Tailwind v4 class+attr 剥离。**built CSS 确认规则命中未剥离**；zh 无 .locale-en 不命中（R1 安全）；全 gate 绿；独立 review agent 0 findings。**@codex rate-limited**→主模型+review agent 终审 | ✅ 合并 main |
| 2026-06-27 | **@codex 补审 #63 SEO 翻旗 + 2 followup** | review-only [#66](https://github.com/sun9bear/AITransVideo/pull/66)（已关）→ [#67](https://github.com/sun9bear/AITransVideo/pull/67) `aa09bfeb` + [#68](https://github.com/sun9bear/AITransVideo/pull/68) `db4e1f75` | @codex 用量恢复后补审已合的 03d-1。**经验：merged PR + 非 main-base PR @codex 都不触发**——故开 review-only PR #66（base=合并前 `087e6fce`、head=`1e93d6fc` 重现 03d-1 diff）才出 review。**2×P2，无 critical/high**：**P2-1**（home `/` 挂 en hreflang，但 `NEXT_PUBLIC_ENABLE_ANONYMOUS_PREVIEW=1` 时 AnonymousTrialPanel 让 /en home 泄漏中文）→ 项目主决策「先 drop /en home」→ **#67**：`localizedRoutes` 移除 `/`；@codex 二审 followup① 又抓出 /en home 残留 broken zh-only alternate set → **followup②** 改为 home/legal 在 page+sitemap **均不挂 hreflang languages**（仅 localizedRoutes 挂），新增 `hreflang-check` home no-en 防回归。**P2-2**（pricing/trial SEO description 硬编码 Free/Plus/Pro/180min/无需绑卡 = gateway 真源漂移）→ **#68**：采纳方案 2 去事实化（`seo.json` zh+en + `zh-snapshot` pin；当前 main 描述在 seo.json、cjk-guard 不扫 messages → 仅 3 文件，无需改 .tsx/baseline）。@codex 对 #67 followup②/#68 均「Didn't find any major issues」。**教训**：spawned task 从主 checkout 起（stale `f1eb671f` −5 PR）会基于旧码、diff 作废 → spawned task 须从 `origin/main` 起 | ✅ 均合并 main |
| 2026-06-27 | **UI-03c legal（zh 源修 + en bilingual）** | [#70](https://github.com/sun9bear/AITransVideo/pull/70) squash `b1cd5ffb` | **HARD 人审单元，项目主二轮人审已签**。zh 源修 4 blocker（terms 7.2 自动续费→非续费一次性周期购买+预留未来产品单独展示 caveat / terms 10.4+privacy 10.1 删百度专条→通用第三方云存储+补「归档工程包可能含用户上传/生成内容」披露 / refund 9.2→原路退回·原支付渠道·微信不写死 / terms 15.2→武汉市洪山区人民法院，与运营主体同区·项目主确认）。3 并行 subagent 译 terms(17 节)/privacy(12 节)/contact(7 节)+legal-page shell，**bilingual-inline**（镜像 refund：zh `<p>`+en `<p>`、标题「中文 / English」、identity 值 verbatim）。我独立核 4 关键条款 en + 全 gate（cjk-baseline 因 title/list/label「zh / en」改动重生成 102±）。**方案=bilingual 而非 dict**（legal 富 JSX dict 化脆弱 + refund 已双语先例）=对 legal **有意 R1 豁免**；legal **保持在 localizedRoutes 之外**（双语页不挂 en hreflang，**原计划「legal 入 localizedRoutes」作废**）。@codex 补审请求多次未稳定落地→项目主二轮人审为终审 | ✅ 合并 main |
| — | **✅ M1 收口（2026-06-27 一会话完成 03e/03f/03d-1/03d-2/03c + 多个 followup/docs）**：**M1 全部单元已合 main，里程碑边界已达**。**下一步 = 停等项目主验收**；不自主跨入 **Phase 2（Wave U-B，UI-05..09）**——受母方案 §9.2 决策门约束。**M1 收尾遗留（owner/后续）**：① **UI-03g** AnonymousTrialPanel 本地化（落地须把 home `/` 加回 `localizedRoutes` + 还原 home hreflang-check/zh-snapshot 断言）；② ~~**SEO 部署门**~~（✅ 2026-07-02 通过，生产实测验收细节见上方 M1 完成段；剩 GSC/Bing 提交 sitemap 归 owner）；③ legal 若想改 locale-switch（zh-only /terms、en-only /en/terms）可用现成 en 文本 refactor 成 dict（可选）。 | — | — | ✅ M1 done |
| 2026-06-29 | **UI-05 App 中央字典本地化（Phase 2 首单元，项目主授权进 Phase 2）** | [#81](https://github.com/sun9bear/AITransVideo/pull/81) squash `efa840ed` | 子 agent 实现（19 文件，status/stage/error/expiry 内联中文 → 新 `app` namespace；translator 首参线程化纯函数 + client `useTranslations('app')`；`JOB_STATUS_LABELS` 对象 → `JOB_STATUSES` key 元组，`JobStatus` 类型不变）。我独立复跑全 gate：cjk-baseline 只减 67、key-parity、zh 字节一致（含新 app）、hreflang、tsc 0、lint 0、build 双 locale。**多 lens 3-agent 对抗评审（R1/R5、parity、scope）0 confirmed**。**CodeX CLI round-1 抓出 1×P1**（前端 gate 看不到的跨语言契约破坏：标签迁 app.json 打断 `test_status_vocab_in_sync` + `test_post_edit_phase0_guards` 的正则守卫）→ 守卫与真源**原子迁移**（解析 `JOB_STATUSES` 元组 + 断言 `app.json status.editing` + 新增 label 覆盖测试），pytest 25 passed、**0 净新增失败**（CI full-suite 确认我的 3 个修复绿）。**codex 双路同时限额**（CLI 10:52PM 恢复 + bot usage limit）→ 主模型终审兜底。CI 阻断项全绿；backend-full-suite 非必需（不设硬门），其红全为 origin/main 既有（UI-02 路径迁移遗留守卫 + t6_policy/assemblyai 历史债）。passthrough：job 标题/视频 id/项目 slug/未映射后端串不译。**遗留单独跟进**：多个 `test_frontend_*` / `test_anonymous_preview_claim_admin_sync_guard` 守卫读 UI-02 迁移前 `app/(app)/...` 路径=origin/main 即红、非本单元——已 spawn 跟进单 | ✅ 合并 main |
| 2026-06-30 | **UI-07 Intl 格式器跟随 active locale** | [#84](https://github.com/sun9bear/AITransVideo/pull/84) squash `fe14353b` | 新 `lib/intl-locale.ts`（intlLocale: zh→zh-CN 字节一致 / en→en-US / 未知→zh-CN）+ `useIntlLocale()`；7 个 always-on 用户页（projects 列表+[jobId] 详情/notifications/settings/voices/billing 2 组件）硬编码 `zh-CN` formatter → active locale；新 `scripts/intl-guard.mjs`（uiloc:intl-guard 进 CI）。R1 运行时证明（zh 三种 formatter 字节一致）。4-lens 对抗评审 2 真 finding 已修（projects/[jobId] 漏改 + guard 多行漏扫→全文件 matchAll）。CodeX CLI 限额→主模型终审。required CI 全绿 | ✅ 合并 main |
| 2026-06-30 | **UI-06 part1 always-on 账户页**（part2 工作台待做） | [#85](https://github.com/sun9bear/AITransVideo/pull/85) squash `99511f6d`（stacked on #84，合 #84 后 rebase→main retarget） | 6 新 namespace（zh+en 共 332 key）：appProjects 71/appVoices 66/appSettings 47/appHelp 18/appNotifications 19/appBilling 111；注册 request.ts+global.d.ts；cjk-baseline 重生成（−381）。6 并行 subagent 本地化→主 integrate + 修 next-intl typed-key（label-map 改 `Record<string,BillingKey>`，tsc 验证 key 真实存在）。gate 全绿；4-lens 对抗评审 **0 crit/high/med**（zh 字节一致/passthrough/动态 key 存在性全过），2 low EN 复数 nit（1 修）。dev-server smoke：/en 全英文、/zh 中文、locale 路由+重定向保 locale、content 透传对（账户页内容鉴权 gate、无后端进不去，build+评审已验）。**part2 核心工作台**（TranslationForm 含1023 consent + workspace 审校 + UI-09）待做 | ✅ 合并 main |
| 2026-06-30 | **UI-09 设计细化**（→ 已实施，见下行） | branch `uiloc/client-error-layer` `afa10233` | 据真实 error 层细化：本地化在**显示层**做（client.ts 无 translator），ApiError 携 errorCode，新 `localizeApiError`，新 `errors` namespace（zh 照搬硬编码=红线1）。纠正母方案 stale（`LOGIN_ERROR_MESSAGES` 已不存在）。UI-09⊥UI-BE-01 解耦。CodeX 建议 UI-09 **前置**于工作台铺文案 | ✅ 实施见下行 |
| 2026-06-30 | **UI-09 客户端错误本地化层（always-on）** | [#86](https://github.com/sun9bear/AITransVideo/pull/86) squash `2efcb57c`（base main） | 机制（client.ts：`ApiError.errorCode` + 导出 `extractErrorCode`/`resolveBackendMessage`[presence-based，与旧内联解析 byte-parity]；timeout payload `{timeoutSeconds}`）+ 新 `lib/api/error-localization.ts`（`localizeApiError` 序 code→**后端 message 先于 status 兜底**→timeout/status 本地化→generic；`useApiErrorMessage` hook useCallback 稳定）+ `messages/{zh,en}/errors.json`（status.*/timeout/generic/status.generic，zh 逐字节照搬 client.ts/errors.ts）。**21 个 always-on 显示点**（projects 列表+详情/voices/settings/billing/notifications/credits-summary）`err.message→localizeError`；工作台 27 点+admin 不动（独立切片）。**Discovery workflow**：40 后端码全在工作台 job-create 路径→`code.*` 本单元不落条目（机制就绪 deferral）。评审：**CodeX 1×R1**（空 message 字节差 `if(backend)`→`!==null`）+ **@codex 1×P2**（非映射 status 漏中文→`status.generic` 兜底）均修；**4-lens 对抗评审 10 findings 全 refuted/o-o-s**（含 `resolveBackendMessage` 270-case byte-parity 0 divergence）。gate：tsc/build/5 守卫全绿（zh-snapshot §8 errors R1 pin）；required CI 全绿，backend-full-suite 非必需红=纯前端 diff 无关。**诚实缺口**：未编码后端错误 en 仍漏中文，UI-BE-01 补。**下一步=工作台薄切片**（路由 27 getErrorMessage+5 硬编码前缀+补 code.*，clone/consent gate） | ✅ 合并 main |
| 2026-06-30 | **UI-06 part2 W1 核心工作台薄切片（任务详情→结果/下载）** | [#87](https://github.com/sun9bear/AITransVideo/pull/87) squash `805cb92c`（base main，branch `uiloc/workbench-w1` 从 origin/main 起） | 5 文件 ~143 CJK（workspace/[jobId]/page 50 + ResultMediaCard 42 + SmartPreviewConfirmDialog 21 + JianyingDraftPathDialog 17 + SmartPreviewResultCard 13）→ 5 新 per-file namespace（appWorkspace/appResultMedia/appSmartPreviewConfirm/appSmartPreviewResult/appJianyingDraft，zh+en，注册 request.ts+global.d.ts）；模块级 helper（sendBrowserNotification/stageLabel/copyToClipboard）translator 首参线程化。**UI-09 接入**：page.tsx 3× getErrorMessage→useApiErrorMessage、JianyingDraftSection catch→localizeError；**无静态 job-create 码命中→不落 errors.code.\***（job-create 属后续切片）。**R1 机械自检 155 片段 0 miss** + zh-snapshot 各 namespace 代表串 pin；cjk-baseline −146（只减）。5 文件无硬编码 Intl formatter→intl-guard IN_SCOPE 不动。**法务**：SmartPreviewConfirmDialog 一条声音权益 consent 行忠实翻译+PR 标疑点（owner 后续完善），clone gate/flag/consent→后端逻辑零改（付费 API 红线保留）。译名：剪映→Jianying（非 CapCut）、完整中文视频→Full dubbed video（中性；zh byte-exact）。**四审全清**：6-lens 对抗 workflow 0 finding + R1 机械自检 0 miss + CodeX CLI no actionable + @codex bot「no major issues」🚀。gate 全绿（tsc/eslint warn-only/next build/5 守卫）；required CI 全绿，backend-full-suite 非必需红=纯前端 diff 无关。**诚实缺口**：未编码后端错误 en 仍漏中文（UI-BE-01）；W2-W5 仍中文；`[jobId]/edit/*` 永久 o-o-s；2 个既有死 import 未动。**下一步建议 W2a**（漏斗入口 chrome） | ✅ 合并 main |
| 2026-06-30 | **UI-06 part2 W2a 创建表单 chrome（漏斗入口）** | [#88](https://github.com/sun9bear/AITransVideo/pull/88) squash `4c0a5d36`（base main，branch `uiloc/workbench-w2a` 从 origin/main 起） | 3 文件（TranslationForm 仅运营 chrome + NewTranslationDialog + translations/new）~110 CJK → 1 新 namespace **appTranslationForm**（zh+en，注册 request.ts+global.d.ts）；模块级 validateYoutubeUrl translator 首参线程化。**法务隔离（plan §2）= 本片核心**：14 条权益/consent/克隆勾选文案全部**留 inline 待 W2b**（免费 1023 attestation+header+校验、YouTube 授权提示、Express 自动克隆勾选框、Smart 付费克隆勾选框）；W2a 零 consent 文案；**克隆/consent 门控逻辑字节不变**（勾选 state / validationError / confirmDisabled / 提交载荷 flags 全不动，付费 API 红线完整）。**UI-09 chokepoint**：handleSubmit getErrorMessage→localizeError + 上传错误路由（行为保持，未编码创建错误仍回后端原文，"still active"/credit gate 判断不破）；**errors.code.\* 拆焦点跟进单**（R1 源是后端 job_intercept.py 中文、与 tsx 两套 provenance，chokepoint 已接将来零返工；静态码清单已提取，consent 码并 W2b，动态码 passthrough）。**R1 机械自检 100 片段 0 miss** + zh-snapshot pin；cjk-baseline −96（只减；14 consent 串仍登记待 W2b）。5 文件无硬编码 Intl→intl-guard 不动。**四审全清**：6-lens 对抗 workflow 0 finding（含 consent-isolation lens）+ R1 自检 0 miss + CodeX CLI 无回归 + @codex bot「no major issues」。gate 全绿（tsc/eslint 仅预存 warn/next build/5 守卫）；required CI 全绿，backend-full-suite 非必需。**诚实缺口**：创建失败错误 + 14 consent 串 en 仍中文（待 code.* 跟进单 + W2b）；W3/W4/W5 仍中文；未部署。**下一片 = W2b（consent，Claude 把关）或 W3（Studio 审校）** | ✅ 合并 main |
| 2026-06-30 | **UI-06 part2 W2b consent/法务文案** | [#89](https://github.com/sun9bear/AITransVideo/pull/89) squash `f5e3526e`（base main，branch `uiloc/workbench-w2b` 从 origin/main 起） | W2a 留 inline 的 **13 条 consent/权益/克隆 opt-in 文案** → 新**独立** namespace **appTranslationFormConsent**（zh+en，便于 owner 单独 review 法务面）；**TranslationForm 至此 0 内联 CJK 完全本地化**。覆盖：免费声音授权 consent（《民法典》1023 attestation+header+校验）+ YouTube 授权提示 + Express 自动克隆勾选框（标题/实验性/描述/3 要点/校验）+ Smart 付费克隆勾选框（标题+attestation，ICU {cost}）。**法务口径=Claude 把关**：en 忠实翻译非市场化法务文本，**疑点已在 PR 标**（① 1023 锚 + "纠纷由我自行承担" 责任转移 → 海外应改 US right-of-publicity / EU GDPR 声纹；② 克隆框缺生物特征数据处理/留存披露），不阻塞 counsel。**硬红线守住**：克隆/consent 门控逻辑字节零改（勾选 state / validationError 条件 / confirmDisabled / 区块渲染条件 / 提交载荷 flags 全不动；付费 API 约束完整，法务放宽不延伸到门控）。R1 机械自检 **17 片段 0 miss**；zh-snapshot **consent 串逐条全 pin**（法务敏感，非抽样）；cjk-baseline 只减。R5 {cost} 仍 ICU 参数。**四审全清**：4-lens 对抗 workflow 0 finding（gate-logic / 忠实翻译 / R1 / typed-key）+ R1 自检 0 miss + CodeX CLI 无回归 + @codex bot「no major issues」。gate 全绿（tsc/eslint 仅预存 warn/next build/5 守卫）；required CI 全绿。**诚实缺口**：创建失败后端错误 en 仍中文（待 errors.code.* 跟进单 + UI-BE-01）；W3/W4/W5 仍中文；未部署。**下一片 = W3（Studio 审校）/ errors.code.* 跟进单 / W4（克隆 modal 文件，含 consent，Claude 把关）** | ✅ 合并 main |
| 2026-06-30 | **UI-06 part2 W3a Studio 审校决策面板**（W3 拆片 a） | [#90](https://github.com/sun9bear/AITransVideo/pull/90) squash `056d6748`（base main，branch `uiloc/workbench-w3a` 从 origin/main 起） | W3（~176 CJK/7 文件）按 plan §7 Q4 拆 W3a+W3b；本片 3 审校决策面板 ~90 CJK → 3 新 per-file namespace（TranslationReviewPanel→appTranslationReview / SmartAutoDecisionPanel→appSmartAutoDecision / VoiceReviewPanel→appVoiceReview）。法务干净。**UI-09 chokepoint**：TranslationReviewPanel 5× getErrorMessage→localizeError（含 plan §3 点名 `拆分失败` 前缀）+ VoiceReviewPanel 提交错误；localizeError（稳定 useCallback）加进所有受影响 effect/useCallback 依赖数组（无 stale-closure/exhaustive-deps 回归）。**模块级 helper 线程化**（part1 约定）：creditsPolicyLabel(ts,…)、statusMeta→纯 classes helper（label 由 StatusPill 用 ts 解析）、Pagination 子组件独立 translator。R5 透传（说话人名/id、音色名、片段/原文/译文、handoff stage/reason、Doubao 2.0 品牌），仅列表分隔符 `、`→`, ` 本地化。无硬编码 Intl（SmartAutoDecisionPanel 用共享 formatTimestamp，其 locale 属 lib/format 独立事项）→ intl-guard 不动。R1 机械自检 **92 片段 0 miss** + zh-snapshot 3 namespace 全 pin；cjk-baseline 只减。errors.code.* 仍延后（errors.json 未动）。**四审全清**：5-lens 对抗 workflow 0 finding（含 UI-09+hook-deps lens）+ CodeX CLI 无回归 + @codex bot「no major issues」。gate 全绿（tsc/eslint 2 预存死代码 unused-var/next build/5 守卫）；required CI 全绿。**诚实缺口**：未编码后端错误 en 仍中文（待 code.* + UI-BE-01）；W3b+W4/W5 仍中文；未部署。**下一片 = W3b（VoiceSelectionPanel+SpeakerAudioAuditModal+EditPageSpeaker* ~86 CJK）** | ✅ 合并 main |
| 2026-07-01 | **UI-06 part2 W3b Studio 音色选择+说话人管理**（W3 拆片 b） | [#91](https://github.com/sun9bear/AITransVideo/pull/91) squash `c716f825`（base main，branch `uiloc/workbench-w3b` 从 origin/main 起） | W3 后半 4 文件 ~86 CJK → 4 新 per-file namespace（VoiceSelectionPanel→appVoiceSelection 60 / SpeakerAudioAuditModal→appSpeakerAudit 12 / EditPageSpeakerCreateDialog→appSpeakerCreate 9 / EditPageSpeakerProfileBadge→appSpeakerBadge 5）。法务干净。**付费红线（最高优先）**：VoiceSelectionPanel 含 CosyVoice 克隆门（`canSpeakerClone`/`cosyvoiceCloneGate.can_show_clone_button`/克隆分发/`approveVoiceSelection`/reassign/dubbing-mode）**逐字未动**，只把展示串换 `t()`；`PROVIDER_SHORT_LABELS` 去掉 `volcengine:'豆包'`、tab label 改 `prov==='volcengine'?t(...)` 三元，provider 集合+分发不变。**UI-09 chokepoint**：VoiceSelectionPanel 3× + SpeakerAudioAuditModal 3× getErrorMessage→useApiErrorMessage，localizeError（稳定 useCallback）加进全部受影响 effect/useCallback deps（无 stale-closure）；EditPageSpeakerCreateDialog 静态 setError 串保持 key（不路由 localizeError，行为保持）。**模块级线程化**（part1 约定）：formatVoiceOptionLabel(t,v)/matchScopeBadge(t,scope) 首参译器；EditPageSpeakerProfileBadge STATUS_MAP 改 typed message key（`Parameters<...>[0]`）由 t(meta.key) 解析，hook 在 early-return 前调用。R5 透传（说话人名/id、音色名、seg.sourceText、点数）；豆包→Doubao 唯一品牌本地化；裸 `{count}` 占位符渲染原值（无千分位）→大数 R1 安全（CodeX 实测证）。跨模板 `warnSpeaker`（3 段模板字面量→1 ICU）渲染逐字节一致。**R1 机械自检 93 片段 0 miss**（2 跨模板片经标点二次校验）+ zh-snapshot 4 namespace pin（section 13）；cjk-baseline −90（4 文件清空，只减）。**四审全清**：5-lens 对抗 workflow **0 finding**（R1/R5/UI-09+hook-deps/typed-key+threading/**paid-api-redline**）+ CodeX CLI「no correctness regression」（含 R5 passthrough + 数字格式 R1 抽验）+ R1 自检 0 miss。gate 全绿（tsc 0/eslint 0 error[3 预存 dead-code warn]/next build/5 守卫）；required CI 全绿（frontend/backend/pg/lint/file-size），backend-full-suite 非必需红=纯前端 diff 无关。**诚实缺口**：未编码后端错误 en 仍中文（待 code.* + UI-BE-01）；W4/W5 仍中文；未部署。**下一片 = W4（克隆 modal 含 consent，Claude 把关）/ W5 / errors.code.* 跟进单** | ✅ 合并 main |
| 2026-07-01 | **UI-06 part2 W4a 克隆 modal + 段选择器**（W4 拆片 a，法务干净） | [#92](https://github.com/sun9bear/AITransVideo/pull/92) squash `d25e1468`（base main，branch `uiloc/workbench-w4a` 从 origin/main 起） | W4 按 W4a+W4b 拆片（consent 隔离到 W4b）；本片 3 clone-UI 文件 ~91 CJK → 3 新 namespace（CosyVoiceCloneModal→appCosyClone 26k / VoiceCloneModal→appVoiceClone 25k / CosyVoiceSegmentPicker→appCosySegments 14k）。**实现委派 Sonnet-5 子 agent，主模型硬核验证**（R1 自检 110 片 0 miss + diff 双向 0 注入逻辑行 + 全 gate）。**付费红线+provider 隔离**：clone gate/authorization/submitCosyvoiceClone/segmentsModeAvailable/endpoint **逐字未动**，只字符串→t()；VoiceCloneModal 零 `cosyvoice` 字面量；phase42 **D2/E1/E2 49/49**（这些 Python 守卫机械强制付费红线）。**顺带修 2 个 stale E1 守卫**：`test_e1_p2_*optgroup*` 原锚定 VoiceSelectionPanel 内联中文 optgroup label，W3b(#91) 迁 t() 后失败（一直红在非阻断 backend-full-suite）→改 locale-aware 锚（key OR 字面量），gate/filter 断言不变、intent 不弱化。**describeCloneError locale-conditional 修 2 个 P2**（CodeX CLI「本地化码被后端中文 message 短路跳过」+ @codex bot「backend 实返 `voice_quota_exceeded`≠switch 的 `quota_exceeded`→errorCode.quotaExceeded 死码」）：zh 仍优先后端中文 message（**字节不变 R1**），en 优先 t(errorCode.*)，未知/动态码回落后端 message；加 useLocale()；映射真实后端码+保留旧 alias。R5 透传（音色名/说话人/价格 ¥0.01/模型名 cosyvoice-v3.5-flash/DashScope/WAV·MP3·M4A/MB·kHz）。UI-09：VoiceCloneModal 2× getErrorMessage→localizeError+deps；另 2 文件静态 setError 保持 key。R1 自检 110 片 0 miss + zh-snapshot §14 pin；cjk-baseline −97（只减）。**四审全清**：6-lens 对抗 workflow **0 finding**（含 paid-api-redline + guard-fix-integrity lens）+ CodeX CLI P2 已修 + @codex bot P2 已修+已复；required CI 全绿（backend 含 phase42 守卫过），backend-full-suite 非必需红=预存 baseline（已少 2 个失败）。**诚实缺口**：未编码/动态后端 clone 错误 en 仍中文（待 errors.code.*+UI-BE-01）；W4b/W5 仍中文；未部署。**下一片 = W4b（CosyVoiceConsentModal 法务 consent + guard-13 修，Claude 把关）/ W5 / errors.code.*** | ✅ 合并 main |
| 2026-07-01 | **UI-06 part2 W4b CosyVoice 克隆授权 consent modal**（W4 拆片 b，法务，Claude 把关） | [#93](https://github.com/sun9bear/AITransVideo/pull/93) squash `178f1e9c`（base main，branch `uiloc/workbench-w4b` 从 origin/main 起） | W4 后半：CosyVoiceConsentModal（声纹生物特征 consent modal，version-pinned modal_version="2026-05-25-v1"）13 法务 consent 串 → 新 namespace **appCosyConsent**（zh 逐字节 + Claude 把关忠实 en）。覆盖：DialogTitle/Description + 3 checkbox（source/data_flow/consequences）title/detail 法务 consent + 付费 API 提示 + cancel/开始克隆 + confirmAllTip。**法务=Claude 把关**（owner §2）：en 忠实翻译，**2 疑点已在 PR 标**（① en consent 挂 **zh-anchored modal_version**——en 用户以英文签署但版本 id 锚中文法务 doc，owner 决定是否需 en 独立 version/法审；② PRC 法框架[侵权/法律责任/账号封禁/依法配合]+境内数据处理[阿里云/华北2-北京/武汉/PIPL]未按海外法域[US right-of-publicity / EU GDPR 声纹跨境]本地化），不阻塞 counsel。**version-pin/付费红线零改**：CONSENT_MODAL_VERSION import+sentinel、onConfirm payload（voice_clone_confirmed/modal_version/confirmed_at）、resetAndClose/handleConfirm/handleOpenChange/allChecked、3 CHECKBOX_IDS 全未动；CHECKBOX_CONFIG 从内联 text 改 **typed message key**（titleKey/detailKey，`Parameters<...>[0]`，source/data_flow/consequences id→key 映射保持）。diff **0 逻辑行删除**；phase42 D2 **guard 2/4/5/13 全过**（version-pin 三层锁/3 checkbox/无付费 API 调用/cancel-reset 路径）。**guard-13 relocalize**：`test_d2_consent_modal_cancel_button_uses_reset_path` 原锚 `取消` 字面量（本片迁 t("cancel")）→改 locale-agnostic（resetAndClose 定义 + onClick={resetAndClose} 存在 + 全文件无 onClick={onClose}），intent 不弱化。**R1 自检 30 片 0 miss** + zh-snapshot §15 **全 13 串逐条 pin**（法务，非抽样）；cjk-baseline −15。**审核**：5-lens 对抗 workflow **legal-faithful lens 抓 1 HIGH**（en consequences.title 把 封禁 误译 "account suspension"[暂时] 与同 checkbox detail 的 "ban"[永久] 自相矛盾+软化法务后果）→改 "account ban" 已修，其余 4 lens 0 finding；**CodeX CLI + @codex bot 双双限额不可用**→主模型唯一终审（precedent）。gate 全绿（tsc 0/eslint 0/next build/5 守卫）；required CI 全绿（backend 含 phase42 过），backend-full-suite 非必需红=预存 baseline。**诚实缺口**：W5 post-edit + errors.code.* 待做；未部署。**下一片 = W5（post-edit）/ errors.code.* 跟进单** | ✅ 合并 main |
